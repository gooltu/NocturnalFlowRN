# Klipy (klipy.com) GIF/sticker API key, used by ChatInputBar's GIF/Sticker tabs.
# Get one from partner.klipy.com. Copy this file to .env.local (git-ignored) and fill it in.
EXPO_PUBLIC_KLIPY_API_KEY=
