/** lucide-react-native is the icon library. Import icons directly from
 * 'lucide-react-native' and size/stroke them with these tokens. */
export const iconTokens = {
  strokeWidth: 2,
  sizeSm: 16,
  sizeMd: 20,
  sizeLg: 24,
} as const;
